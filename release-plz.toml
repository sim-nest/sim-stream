# release-plz opens version and changelog proposals for publishable workspace
# crates. Repository release workflows are managed explicitly.
[workspace]
# Keep workspace crate versions synchronized when a release is prepared.
dependencies_update = true
git_release_enable = true
# release-plz computes publish order from cargo metadata.
