# XR pose sample

A physical glasses rig is not available inside the sandbox. This descriptor
shows the real sample shape modeled sources emit in tests: an XR pose sample
kind, a monotone sequence number, pose timing, optional position, orientation,
tracking status, and prediction horizon.
