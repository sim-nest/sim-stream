# Stream memory pipe (descriptor)

Documents a memory-source to memory-sink stream pipe -- the minimal in-memory wiring. The pipe runs
through the stream fabric outside the cookbook sandbox eval stack, so it is documented rather than run.
