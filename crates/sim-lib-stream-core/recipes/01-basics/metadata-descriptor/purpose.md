# Stream metadata shape

A live stream needs a running fabric, which the sandbox does not stand up. This recipe is a **descriptor** (tagged `sandbox-descriptor`): it shows the real
surface shape rather than a live result, because that result cannot be reproduced in
the cookbook's read-eval sandbox.
