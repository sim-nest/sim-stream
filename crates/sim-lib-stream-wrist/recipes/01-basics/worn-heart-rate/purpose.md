# Worn heart-rate sample

A wrist device is not available inside the sandbox. This descriptor shows the
sample shape modeled sources emit in tests: a worn-event sample kind, a
monotone sequence number, a heart-rate sensor tag, a confidence score, and a
sensor payload.
