# Stream clock tempo chart (descriptor)

Documents the stream clock's tempo chart: the mapping from ticks to samples at a tempo. It is a timing
map, not an expression the sandbox eval stack reduces to a value.
