# Device capabilities sample

A physical device is not available inside the sandbox. This descriptor shows
the real sample shape that modeled sources emit in tests: a stable sample kind,
a monotone sequence number, and symbol lists for streams, inputs, and outputs.
